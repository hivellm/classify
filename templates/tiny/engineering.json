{
  "$schema": "https://hivellm.org/schemas/classify-template-tiny-v1.json",
  "version": "1.0",
  
  "metadata": {
    "name": "engineering",
    "display_name": "Engineering (Tiny)",
    "description": "Lightweight template for technical documents and code",
    "domains": ["engineering", "technical", "software"],
    "priority": 85
  },
  
  "llm_config": {
    "provider": "deepseek",
    "model": "deepseek-chat",
    "temperature": 0.0,
    "max_tokens": 500,
    "system_prompt": "Extract title, language, and main components. Keep it minimal."
  },
  
  "entity_definitions": [
    {
      "type": "Document",
      "properties": {
        "title": { "type": "string", "required": true },
        "language": { "type": "string", "required": false }
      }
    },
    {
      "type": "Component",
      "properties": {
        "name": { "type": "string", "required": true },
        "type": { "type": "string", "required": false }
      }
    }
  ],
  
  "relationship_definitions": [
    {
      "type": "HAS_COMPONENT",
      "source": "Document",
      "target": "Component"
    }
  ],
  
  "graph_schema": {
    "nodes": [
      {
        "type": "Document",
        "labels": ["Document", "Technical"],
        "required_properties": ["id", "title"],
        "indexes": [
          { "property": "id", "type": "unique" }
        ]
      },
      {
        "type": "Component",
        "labels": ["Component"],
        "required_properties": ["name"],
        "indexes": [
          { "property": "name", "type": "index" }
        ]
      }
    ],
    "relationships": [
      {
        "type": "HAS_COMPONENT",
        "source": "Document",
        "target": "Component"
      }
    ]
  },
  
  "fulltext_schema": {
    "required_fields": ["title", "domain", "doc_type"],
    
    "extracted_fields": [
      {
        "name": "language",
        "type": "string",
        "extraction_method": "llm",
        "index": true
      },
      {
        "name": "components",
        "type": "array",
        "extraction_method": "llm",
        "index": true
      }
    ],
    
    "keyword_extraction": {
      "enabled": true,
      "method": "tfidf",
      "min_frequency": 2,
      "max_keywords": 8
    },
    
    "summary": {
      "enabled": true,
      "method": "llm",
      "max_length": 100
    }
  },
  
  "extraction_rules": [
    {
      "name": "extract_tech",
      "method": "llm",
      "prompt": "Extract language and 2-3 main components"
    }
  ],
  
  "validation_rules": [
    {
      "field": "title",
      "rules": [
        { "type": "required" },
        { "type": "max_length", "value": 200 }
      ]
    }
  ]
}

